## Scan CDN proxy

Scan the survival status of CDN proxy node

 Determine which IP are real hosts

#### Usege:

```python
# IP list
    proxy_ips = [
        "x.x.x.x",
    ] //replace you want IP
```

